node_modules/
.expo/
dist/
web-build/
android/app/build/
android/build/
android/.gradle/
android/app/.cxx/
ios/Pods/
ios/build/

# Env files — never commit secrets
.env
.env.local
*.keystore
!debug.keystore

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Freebuff local state
.freebuff/
.idea/

# goldie store-asset outputs (config + flows are committed)
goldie/out/
goldie/*.apk

# local secrets - never push
.env.prod-keep
.claude/
key.properties
